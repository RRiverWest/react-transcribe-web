server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React (SPA) の静的配信のみ。api へは React が
    # VITE_SOCKET_URL のオリジンへ直接接続する。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
